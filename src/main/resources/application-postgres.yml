spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    generate-ddl: false
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/cinema
    username: postgres
    password: mypass
    driverClassName: org.postgresql.Driver
